useRef는 어떤 DOM으로의 포커스 처럼 
특정 DOM을 선택할 때 사용하는 리액트 훅으로 
어떤 DOM에 이름을 붙여 그 이름으로 호출하는 용도로 사용된다. 

혹은 컴포넌트 안에서 조회 및 수정 가능한 변수를 관리하는 용도로도 사용되는데 
이 때 useRef 안의 변수가 업데이트 된다고 해서 컴포넌트가 리렌더링이 되지 않는다. 
즉 굳이 리렌더링 할 필요가 없는 변수라면 useRef로 관리해주는 것이 효율적이다. 