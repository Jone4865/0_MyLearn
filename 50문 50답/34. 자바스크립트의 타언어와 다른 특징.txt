(1) 데이터 타입의 전환이 자유로움, 변수에 할당되는 값에 따라 변수의 타입이 결정
(2) 프로토타입 기반의 객체지향 언어: 객체끼리의 chaining을 통해 동적으로 표현
(3) 동기적인 특성을 가져서 작성된 순서대로 실행된다.
(4) 스크립트 언어: 실행환경에서 추가적인 환결 설정이 필요 없고 브라우저의 동작에 따라간다
(5) 실행을 위해서 html, css에 의존적이다.